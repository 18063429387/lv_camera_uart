# Camera preview and UART agreement test

This directory builds a small Linux target program for camera preview and UART agreement handling.

The LVGL UI, LVGL framebuffer/input drivers, and LVGL touch adapter have been removed.
